import React from 'react'
import { PropertyAPIInput } from 'defs'

type Props = {
  propertyId: string
  propertyInfo: PropertyAPIInput
}

export const PropertyInfoDrawer: React.FunctionComponent<Props> = ({ propertyId, propertyInfo }) =>
  null
